import { useEffect, useState } from "react";


const CommentLike = ({ commentId }) => {
     const [likeCount, setLikeCount] = useState(0);
    
        // useEffect(() => {
        //     axios.get(`${url}/comment/like`, { headers: { token } })
               
    
        // }, [commentId]);

}

export default CommentLike;